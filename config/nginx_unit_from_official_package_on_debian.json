{
    "name": "nginx_unit",
    "detect": {
        "type": "shell",
        "command": "/usr/sbin/unitd --version 2>&1 | grep 'unit version:' | awk '{print $3}'"
    },
    "cpe": {
        "app": "unit",
        "manufacturer": "nginx"
    }
}